name: reviews
description: "A new Flutter package project."
version: 0.0.1

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dartz: ^0.10.1
  freezed_annotation: ^3.0.0
  kt_dart: ^1.1.0
  injectable: ^2.5.0
  core: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  freezed: ^3.0.0
  build_runner: ^2.4.15
  injectable_generator: ^2.7.0
